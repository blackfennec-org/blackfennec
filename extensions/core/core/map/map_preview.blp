using Gtk 4.0;
using Adw 1;

template MapPreview : Button {
  focusable: true;
  receives-default: true;
  halign: end;
  valign: center;
  clicked => _on_navigate_clicked();

  icon-name: "go-next-symbolic";
  styles [
    "flat",
    "circular"
  ]
}
