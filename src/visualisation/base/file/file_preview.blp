using Gtk 4.0;
using Adw 1;

template FilePreview : Button {
  icon-name: "emblem-documents-symbolic";
  clicked => _on_navigate();

  valign: center;
  visible: true;
  focusable: false;

  styles [
    "flat",
    "circular",
  ]
}