using Gtk 4.0;
using Adw 1;

template ListItemView : Adw.ActionRow {
  visible: true;
  focusable: false;
  activatable-widget: _preview_container;

  Box _preview_container {
    orientation: vertical;
    focusable: false;
    valign: center;
  }
}